<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.polly.pollyapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity
            android:name=".AwesomeActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SeniorHighSchoolActivity" />
        <activity android:name=".JuniorHighSchoolActivity" />
        <activity android:name=".MeActivity" />
        <activity android:name=".PrimaryActivity" />
        <activity android:name=".UniversityActivity"></activity>
    </application>

</manifest>